# Upstream Clone Status

The requested upstream repositories were not present in `refs/upstream/`.

Clone attempts were made for:

- `https://github.com/Shinei-Nouzen-Arch/FPGA-Agent`
- `https://github.com/Shinei-Nouzen-Arch/GEMM-HLS`

Both attempts failed because GitHub connections were reset or unreachable from
this environment. The method migration was completed from GitHub raw source
views where available. The `refs/upstream/` directory remains available for a
future successful clone.

